#include <stdio.h>
/**
Read the two digits from the standard in put
digi 1 = input()
digit 2 = input()

/* convert the digits from strings to integers */
digit 1 = int(digit 1)
digit 2 = int(digit 2)

/*find the reminder of the sum when divide by 10 */
reminder = total % 10
print(remainder)
